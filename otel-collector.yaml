receivers:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  debug:
    verbosity: normal
  file:
    path: ${OTEL_LOG_PATH}
  file/traces:
    path: ${OTEL_TRACES_PATH}

service:
  pipelines:
    logs:
      receivers: [otlp]
      exporters: [file]
    traces:
      receivers: [otlp]
      exporters: [debug, file/traces]
